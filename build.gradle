plugins {
    id "java"
}

group "co.jaqobb"
version "2.0.0"

description "A library to inject dependencies directly into your Java code without increasing jar file size"

sourceCompatibility = JavaVersion.VERSION_1_10
targetCompatibility = JavaVersion.VERSION_1_10

defaultTasks "clean", "build"